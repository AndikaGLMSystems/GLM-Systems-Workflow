trigger:
- master

pool:
  vmImage: 'vs2017-win2016'

steps: 
- task: katalonTask@0
  inputs:
    version: '8.0.1'
    executeArgs: '-browserType="Chrome" -retry=0 -statusDelay=15 -testSuitePath="Test Suites/Regression Suite/Test Suite Collection/TS Collection for parallel run.ts" -apiKey="$(KATALON_API_KEY)"'